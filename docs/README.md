# Documentation

Modular docs for the Stanford Stroke Center PACS repo.

**Start here:** [`context.md`](context.md) — short map so you can attach only the files an agent needs.

Canonical docs live under `reference/`, `guides/`, `operations/`, `cold_storage/`, and `recipes/`. `history/` is a frozen historical archive (superseded plans + a dated changelog snapshot), not current-truth documentation.
