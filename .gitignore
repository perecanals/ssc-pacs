# Secrets / auth
.env
**/.env
orthanc_users.json
**/orthanc_users.json
maintenance
hidden
benchmarks
*.sql

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# IDE / editor
.cursor/
.vscode/
.claude/
.idea/

# Frontend build artifacts
**/node_modules/
**/dist/

# Test / coverage artifacts
.coverage
**/.coverage
htmlcov/

# Benchmarks / temp
.tmp_bench/
**/.tmp_bench/
stanford-stroke-pacs/benchmarks/
rsync_backup_remote_benchmark_results.json

# Migration/backup dumps
migration_backups/

# Local maintenance notes (not part of shipped repo)
maintenance/

# Integration protocol logs
stanford-stroke-pacs/image_integration_protocols/logs/

# SQL data dumps (schema only — CSV/raw data stays out)
ssc-sql-db/tables/

# OS
.DS_Store
Thumbs.db

# Embedded upstream VCS metadata (orthanc-indexer-patched vendors Mercurial source)
.hg/
**/.hg/

# Deprecated compose auto-merge override. macOS deltas now live in the tracked
# docker-compose.override.macos.yml, selected explicitly by scripts/orthanc/dc.sh.
# Ignore the auto-merge name so a stray copy never silently breaks a Linux host.
docker-compose.override.yml
**/docker-compose.override.yml

# Per-host deployment identity overrides for the unit installers (auto-derived
# by default; see deploy.env.example).
deploy.env
**/deploy.env

# Site/run-specific ingestion config (use the .example.yaml template)
stanford-stroke-pacs/image_integration_protocols/execute_image_integration_protocol.yaml
# Per-batch configs derived from the base YAML by run_all_batches.sh
stanford-stroke-pacs/image_integration_protocols/batch_configs/
