# Progress tracker

Single source of truth for workstream status. Update this file whenever you
start, finish, or block on a workstream.

**Status values:** `todo | in_progress | blocked | done`

---

## Phase A — Risk reduction (P0)

| # | Workstream | Status | Owner | Started | Finished | Commit | Notes |
|---|---|---|---|---|---|---|---|
| 01 | [Backup and DR](workstreams/01-backup-and-dr.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | SQL-only on dev (no /DATA3); Tier 2 dormant; pg-backup-{stanford-stroke,orthanc,freshness}.timer installed and active on host (DICOM mirror timer correctly inactive — Tier 2 dormant on dev) |
| 02 | [Dependency pinning](workstreams/02-dependency-pinning.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | docker-compose.yml not touched: already uses local `ssc-orthanc:patched-indexer` tag, upstream pin lives in Dockerfile |
| 03 | [Security hardening](workstreams/03-security-hardening.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | All 5 controls verified end-to-end; production `ssc-companion` on :8043 left untouched; T7 (CI grep) handed off to WS 07 |

## Phase B — Operability (P1)

| # | Workstream | Status | Owner | Started | Finished | Commit | Notes |
|---|---|---|---|---|---|---|---|
| 04 | [Schema migrations](workstreams/04-schema-migrations.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | Single combined baseline (T4 split deferred — see notes); prod stamp pending |
| 05 | [Cold-storage robustness](workstreams/05-cold-storage-robustness.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | Alembic 0002 applied to dev DB; systemd units written but **not installed** (operator action); ssc-companion **not restarted** (changes sit on disk for explicit rollout) |
| 06 | [Observability](workstreams/06-observability.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | JSON structured logging, /healthz, /metrics, request-ID middleware, Grafana dashboard; journal rotation documented (not provisioned) |
| 07 | [CI and testing](workstreams/07-ci-and-testing.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | 28 backend tests (65% cov), 10 frontend tests; ruff+pre-commit; CI workflow; Makefile; T9 (prettier mass-format) deferred to separate commit |
| 08 | [Deployment portability](workstreams/08-deployment-portability.md) | todo | — | — | — | — | — |

## Phase C — Structural debt (P2)

| # | Workstream | Status | Owner | Started | Finished | Commit | Notes |
|---|---|---|---|---|---|---|---|
| 09 | [Backend refactor](workstreams/09-backend-refactor.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | app.py 234 lines (was 2,062); 8 route modules; unified label filter SQL; DB pool; 35 tests pass |
| 10 | [Frontend refactor](workstreams/10-frontend-refactor.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | DataTable 1,272→498 lines (max); 8 files in DataTable/; shared utils extracted; PropTypes (not TypeScript — decision recorded below); 39 vitest pass |
| 11 | [Data integrity (two-DB)](workstreams/11-data-integrity-two-db.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | All 9 tasks complete; Prometheus metrics added (WS 06 done); systemd units written but not installed; verify_indexing.py deprecated |
| 12 | [Annotation audit trail](workstreams/12-annotation-audit-trail.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | All tasks complete (T10 UI deferred); 43/43 pytest, 39/39 vitest, ruff clean, build clean |
| 13 | [Scripts reorganization](workstreams/13-scripts-reorganization.md) | done | claude (opus 4.6) | 2026-04-15 | 2026-04-15 | uncommitted | 8 subdirectories; verify_indexing.py deleted; test_snapshot_sql.py moved to tests/; label_studies.py ENV_PATH bug fixed; all docs + systemd units updated |

---

## Changelog

Append-only log of status transitions. Newest at top.

- **2026-04-15** — WS13 (Scripts reorganization) → `done` (claude). Reorganized flat `scripts/` (25 files) into 8 subdirectories: `admin/`, `backup/`, `cold_storage/`, `connectivity/`, `data_integrity/`, `dicom/`, `one_off/`, `orthanc/`. Deleted deprecated `verify_indexing.py` and stray `verify_and_repair_archives_results.json`. Moved `test_snapshot_sql.py` to `companion/tests/`. Fixed `label_studies.py` `ENV_PATH` NameError. Updated REPO_ROOT path computations in all 14 moved Python scripts. Updated all documentation (~12 files), 6 systemd units, CLAUDE.md, and maintenance workstream files. Added `scripts/README.md` index. **Not yet committed.**
- **2026-04-15** — WS12 (Annotation audit trail) → `done` (claude). **T1 decision: trigger-based capture** — impossible to bypass, UPSERT fires correct trigger automatically, no CRUD code changes needed. **T2–T3:** Alembic revision `0003_annotations_history` — creates `annotations_history` table (append-only, BIGSERIAL PK), PL/pgSQL `annotations_audit()` function, and `annotations_audit_trg` trigger (AFTER INSERT OR UPDATE OR DELETE). Trigger resolves `entity_id` from level (patient_id/studyinstanceuid/seriesinstanceuid), captures `value_before`/`value_after` and `notes_before`/`notes_after`, reads `current_setting('app.audit_user', true)` for attribution (falls back to `'system'`). Two indexes: `(annotation_id, operation_at DESC)` and `(entity_id, operation_at DESC)`. **T4:** Added `audit_user_var` contextvar to `db.py`; `get_conn()` auto-sets `SET LOCAL app.audit_user` from the contextvar. Middleware in `app.py` sets the contextvar from the JWT cookie alongside the existing `user_ctx`. **T5:** `GET /api/annotations/{annotation_id}/history` endpoint in `routes/annotations.py` — admin-only via `_require_admin()`, returns history rows newest-first. Route table test updated. **T6:** `scripts/backfill_annotation_history.py` — inserts one synthetic `I` row per annotation with no history; idempotent (`NOT IN` check); `--dry-run`/`--execute` flags; tested against dev DB (546 annotations found). **T7:** 8 new tests in `test_annotation_history.py`: trigger INSERT/UPDATE/DELETE/UPSERT capture, fallback to 'system', admin endpoint access, non-admin 403, newest-first sort order. **T8–T9:** `documentation/operations/annotation_history.md` (schema, trigger mechanics, session-variable coupling, API, backfill, retention policy, rollback). Updated `data_stores.md` with `annotations_history` table DDL. Linked from `context.md`. **T10:** UI affordance deferred (time-boxed out — can be a follow-up). **Verification:** 43/43 pytest pass, 39/39 vitest pass, ruff clean, `npm run build` clean. Trigger verified via manual INSERT → history row with correct `op=I by=tester`. Alembic migration applied to dev DB. Production `ssc-companion` **not restarted**. **Not yet committed.**
- **2026-04-15** — WS12 (Annotation audit trail) → `in_progress` (claude). WS 04 dep verified done. T1: trigger-based capture.
- **2026-04-15** — WS11 (Data integrity: two-DB reconciliation) → `done` (claude). T1: inventoried `scripts/verify_indexing.py` (compares image_series vs Orthanc REST — has ENV_PATH bug, uses inline DB config) and `scripts/list_unarchived_series.py` (flags series with no `dicom_archive_path`). T2: new `companion/reconciliation.py` — `diff_image_series_vs_orthanc(conn, session)` fetches all DB series + all Orthanc series UIDs, classifies mismatches into 4 categories (`in_db_not_in_orthanc`, `in_orthanc_not_in_db`, `dicom_dir_missing`, `dicom_archive_missing`); `snapshot_summary(report)` extracts counts. T3: new `scripts/reconcile.py` — CLI wrapper with `--json` (writes to `maintenance/reconciliation-reports/YYYY-MM-DDTHHMMSS.json`, rotates to last 30) and `--quiet` flags; human-readable output by default. T4: new `GET /api/admin/reconciliation/latest` in `routes/admin.py` — reads latest JSON report; admin-only via `_require_admin()` helper checking `users.is_admin`; non-admins get 403. T5: added 3 Prometheus gauges to `metrics.py`: `reconciliation_mismatches_total{category}`, `reconciliation_last_run_timestamp`, `reconciliation_duration_seconds`; `update_reconciliation_metrics()` called by CLI on every run. T6: `systemd/reconciliation.{service,timer}` — runs `reconcile.py --json --quiet` every 6h; **not installed** in `/etc/systemd/system/` — operator action required. T7: new `documentation/operations/reconciliation.md` — mismatch categories explained with investigation/repair procedures, Prometheus metrics catalogue, systemd install instructions, related scripts; linked from `context.md`. T8: deprecated `scripts/verify_indexing.py` with `DeprecationWarning` and docstring note pointing to `reconcile.py`; fixed pre-existing `ENV_PATH` NameError bug. T9: seeded `test-uid-not-in-orthanc` → detected in `in_db_not_in_orthanc` (count 38→39); verified in JSON output; cleaned up. Updated `test_route_table.py` to include new endpoint. **Verification:** 35/35 pytest pass, 39/39 vitest pass, ruff clean. Production `ssc-companion` **not restarted**. **Not yet committed.**
- **2026-04-15** — WS11 (Data integrity: two-DB reconciliation) → `in_progress` (claude). WS 06 dep verified done. Tasks T1–T9.
- **2026-04-15** — WS10 (Frontend refactor) → `done` (claude). **T11 decision: PropTypes** — chose PropTypes over TypeScript. Rationale: zero build-chain changes, runtime validation, covers component API documentation. TypeScript can be a future workstream. **Phase A (T1–T4):** new `src/utils/colors.js` — single-source `NOTION_COLORS`, `hashStr`, `valueColor` (was duplicated in InlineEdit + LabelDefModal). New `src/utils/table.js` — `LEVEL_CONFIG`, `LEVEL_RANK`, `LEVEL_ORDER`, `PER_PAGE`, `buildBuiltinColumnCatalog`, `formatDatetime`, `buildPatientStudiesUrl`, `normalizeSelectFilterValues`, `hasFilterValue`, `getTextFilterValue`. Updated InlineEdit + LabelDefModal to import from utils. 2 new vitest test files: `colors.test.js` (7 tests), `table.test.js` (22 tests). **Phase B (T5–T10):** split `DataTable.jsx` (1,272 lines) into `DataTable/` directory with 8 files: `index.jsx` (498 lines, orchestrator + main body), `ChildRows.jsx` (284 lines, child+grandchild table rendering), `TableHeader.jsx` (169 lines, thead with column headers + filter row), `SelectFilterControl.jsx` (79 lines, dropdown filter), `useTableData.js` (78 lines, data fetch hook), `usePreferencePersistence.js` (70 lines, debounced pref save), `useDragColumns.js` (62 lines, column drag-reorder), `actions.js` (41 lines, DICOM download + OHIF link + refresh actions). Old `DataTable.jsx` deleted. Import from `Companion.jsx` resolves unchanged. Widened vitest include to `src/**/__tests__/**`. **Phase C (T12):** added `prop-types` to dependencies. Added `propTypes` blocks to: DataTable, ChildRows, TableHeader, SelectFilterControl, InlineEdit, LabelDefModal, TopBar, Sidebar, PreviewPane, ColumnSelector, Pagination. **Phase D (T13–T14):** `npm run build` clean, 39/39 vitest pass (was 10), no component >500 lines, `NOTION_COLORS` defined once. Production `ssc-companion` **not restarted**. **Not yet committed.**
- **2026-04-15** — WS10 (Frontend refactor) → `in_progress` (claude). WS 07 dep verified done. Phases A→B→C→D.
- **2026-04-15** — WS09 (Backend refactor) → `done` (claude). **Phase A (T1–T4):** new `companion/db.py` — centralized `DB_CONFIG`, `_require_env`, `_PooledConnection` wrapper (intercepts `.close()` → `putconn()`), `ThreadedConnectionPool` (min=2, max=10), `init_pool()`/`close_pool()` lifecycle, `get_conn()` returns pooled or plain connection. New `companion/orthanc_client.py` — `orthanc_lookup()` and `orthanc_system_check()` wrapping Orthanc REST calls. New `companion/auth.py` — JWT utilities (`create_jwt`, `decode_jwt`, `get_current_user`, `get_optional_user`) extracted from app.py. New `companion/__init__.py` (empty, makes it a package). Replaced inline `DB_CONFIG` in `cache_manager.py` (aliased `_conn = get_conn`) and 11 scripts under `scripts/`. Updated `conftest.py` to patch `db.DB_CONFIG` once (instead of `app.DB_CONFIG` + `cm.DB_CONFIG`). **Phase B (T5–T7):** new `companion/common.py` — `build_label_filter_sql(entity_level, label_level, entity_id_expr, operator, value_predicate)` replaces four 95%-duplicated helpers using two lookup tables (`_PARENT_COL` for filtering UP, `_DOWN_JOIN` for filtering DOWN). Also holds `parse_label_filters`, `apply_label_filters`, `attach_annotations`, `attach_inherited_annotations`, shared constants. 6 parametric tests verify SQL output matches the old helpers for all 9×4 combos. **Phase C (T8–T11):** 8 route modules under `companion/routes/`: `auth.py` (login/logout/me), `preferences.py` (GET/PUT prefs), `studies.py` (patient/study/series browsing, OHIF link, DICOM zip, import labels), `cold_storage.py` (warm/evict/cache-status/storage-mode), `annotations.py` (CRUD), `labels.py` (label defs, summary, values, snapshot/labelled-table refresh), `admin.py` (/healthz, /metrics), `static.py` (SPA catch-all). `app.py` shrunk from 2,062 → 234 lines: keeps only lifespan (pool+migration), middleware (sliding JWT, request-ID/metrics), rate limiter, router registration. Route table snapshot test (`test_route_table.py`) asserts all 30 endpoint paths are preserved. **Phase D (T12):** 35/35 pytest pass, 10/10 vitest pass, `npm run build` succeeds, ruff clean. T13 (pool soak) deferred to production rollout. Production `ssc-companion` **not restarted**. **Not yet committed.**
- **2026-04-15** — WS09 (Backend refactor) → `in_progress` (claude). WS 07 dep verified done. Phases A→B→C→D.
- **2026-04-15** — WS07 (CI and testing) → `done` (claude). T1: new `companion/requirements-dev.txt` with pinned `pytest==9.0.3`, `pytest-asyncio==1.3.0`, `pytest-cov==7.1.0`, `httpx==0.28.1`, `ruff==0.15.10`, `mypy==1.20.1`. T2: new `companion/pyproject.toml` with `[tool.ruff]` (line-length 120, py312, E/W/F/I/B/UP rules, E402/B904/B008 ignored), `[tool.pytest.ini_options]` (asyncio_mode=auto), `[tool.mypy]` (ignore_missing_imports). T3: new `companion/tests/conftest.py` — session-scoped `test_db` fixture creates scratch DB `test_stanford_stroke` via `CREATE DATABASE`, runs `alembic upgrade head`, seeds test user + reference data; `client` fixture patches `DB_CONFIG` in both `app` and `cache_manager`, disables rate limiter and Secure cookies for TestClient compatibility; `logged_in_client` logs in as test user. T4: 28 backend tests across 5 files: `test_auth.py` (7 tests: login success/failure, /api/me, logout, session refresh, protected endpoints), `test_annotations.py` (9 tests: create/read/update/delete at patient/study/series levels, inheritance, validation), `test_labels.py` (7 tests: create/list/duplicate/invalid-name/invalid-datatype/select-with-options), `test_health.py` (2 tests: healthy 200, degraded 503), `test_cold_storage.py` (2 tests: warm→hot→evict→cold cycle with mock tar.zst archive, no-archives error). Coverage: 65% overall, app.py 51%, cache_manager.py 61%, config.py 97%. T5: added `vitest==4.1.4`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` to `companion/package.json` devDeps; added `test` and `test:watch` scripts. T6: new `companion/vitest.config.js` (jsdom, globals, setup file, v8 coverage). T7: 10 frontend tests across 3 files: `apiClient.test.js` (7 tests: URL handling, 401 event dispatch, apiGet/apiPost/apiDelete), `Landing.smoke.test.jsx` (2 tests: renders title + cards), `Companion.smoke.test.jsx` (1 test: renders with mocked API). T8: new `.pre-commit-config.yaml` at repo root with ruff, prettier, trailing-whitespace, end-of-file-fixer, check-yaml. T9: ruff auto-fixed import ordering in 5 files; ruff now passes clean. Prettier mass-format deferred to a separate commit (blame cleanliness per workstream notes). T10: new `.github/workflows/ci.yml` — 5 jobs (lint, backend-tests with Postgres service container, frontend-tests, frontend-build, mypy advisory/non-blocking); all required except mypy. T11: new `Makefile` at repo root with targets: install-dev, lint, test, test-backend, test-frontend, build, help. T12: added "Developer setup" §8 to `installation_and_deployment.md` (prerequisites, make install-dev, make test, CI description). Verification: ruff clean, 28/28 pytest pass (65% cov), 10/10 vitest pass, `npm run build` succeeds. **Not yet committed.**
- **2026-04-15** — WS07 (CI and testing) → `in_progress` (claude). No deps; backend + frontend test harnesses + CI pipeline.
- **2026-04-15** — WS06 (Observability) → `done` (claude). T1: pinned `python-json-logger==4.1.0` + `prometheus-client==0.25.0` in `companion/requirements.txt`. T2: new `companion/logging_config.py` — JSON formatter via `python-json-logger`, two contextvars (`request_id_ctx`, `user_ctx`) injected via `ContextFilter`; `configure_logging()` re-enables loggers disabled by Alembic's `fileConfig()`. T3: replaced `_log = logging.getLogger("uvicorn.error")` with `logger = logging.getLogger(__name__)` in `app.py`; `cache_manager.py` already migrated in WS 05. T4: added `request_id_middleware` (outermost ASGI layer) — generates UUID per request, populates contextvars, logs `method/path/status/duration/user`, sets `X-Request-ID` response header, increments Prometheus request counters+histogram. T5: added `GET /healthz` (unauthenticated) — checks `stanford-stroke` DB, `orthanc_db` (via `PG_ORTHANC_*` env vars), Orthanc HTTP API, disk free; returns 503 on critical failure. T6: new `companion/metrics.py` (added to allowlist) — Prometheus registry with `http_requests_total`, `http_request_duration_seconds`, `cold_storage_{warm,evict}_total`, `cold_storage_warming_rows` (gauge), `cold_storage_disk_free_bytes` (gauge); `GET /metrics` endpoint refreshes gauges on scrape. T7: chose journal approach — documented `journald.conf` `SystemMaxUse=2G` / `MaxRetentionSec=30day` recommendations in observability.md; `ssc-companion.logrotate` not created (not needed with journal). T8: committed starter Grafana dashboard JSON at `documentation/operations/grafana_dashboard.json` — 5 panels: request rate, p50/p95 latency, warm/evict success rate, cold-storage disk free, warming rows. T9: new `documentation/operations/observability.md` — log format, /healthz semantics, metrics catalog, Prometheus scrape config, recommended alert rules (pseudo-PromQL). T10: linked from `documentation/context.md`. Verification: all 4 checks pass — JSON logs with request_id, X-Request-ID propagation, /healthz 200 + 503 on DB failure, /metrics with all starter metrics. Production `ssc-companion` **not restarted** — changes sit on disk for explicit rollout. **Not yet committed.**
- **2026-04-15** — WS06 (Observability) → `in_progress` (claude). No deps; WS 05 already landed so warm/evict logging conventions can be reused.
- **2026-04-15** — WS05 (Cold-storage robustness) → `done` (claude). T1: new Alembic revision `companion/alembic/versions/0002_warming_started_at.py` adds `cache_state.warming_started_at TIMESTAMPTZ`; applied to dev DB (`alembic current` → `0002_warming_started_at (head)`). T2: added `warming_timeout_minutes` (default 30), `warming_disk_safety_factor` (default 3.0), `warming_disk_min_free_bytes` (default 100 MiB) to `[storage]` in `config.toml` and `companion/config.py`. T3: stuck-warming watchdog in `warm_study()` — when row is `status='warming'` past the timeout, log a structured warning and proceed to re-warm. Verified by injecting a 90-min-old `warming` row and calling `warm_study`; watchdog fires (`age=5400s > timeout=1800s`). T4: pre-extraction disk-space check estimates `safety_factor × Σ(compressed_size) + min_free_bytes`, raises new `InsufficientDiskSpaceError(required, available, target)`, marks the row as `cold` (not `warming`), surfaces `507 Insufficient Storage` from `/api/studies/{uid}/warm`. Verified by monkey-patching `_disk_free_bytes` to 1; row reset to `cold`, no `.warming` dirs left behind. T5: `evict_study()` is now transactional (filesystem rmtree first; `cache_state` row deleted only after all rmtrees succeed; failure leaves the row intact and re-raises). API surfaces `500 evict_failed`. Verified by `chmod 500` on a series dir → exception raised, row stays `hot`; after `chmod 755`, retry succeeds and row is deleted. T6: `cache_manager.py` now uses module-level `logger = logging.getLogger(__name__)`; every warm/evict log line carries `extra={'study_uid': uid}`. `app.py:_eviction_loop` emits a per-study log line in addition to the rollup. T7: tmp-dir cleanup at the warm-failure path no longer swallows errors — it logs them with study UID. T8: new `scripts/cold_storage_health.py` — reports stuck-warming rows, orphan `*.warming` dirs, free disk on `legacy_dicom_root`, and `cache_state` last-accessed distribution; `--json` for machine output, `--quiet` for cron, exits non-zero on critical conditions. T9: `systemd/cold-storage-health.{service,timer}` written (15 min cadence; uses `/home/perecanals/miniconda3/envs/pacs/bin/python`). **Timer not installed** in `/etc/systemd/system/` — operator must `cp` and `systemctl enable --now`. T10: `documentation/cold_storage/runbook.md` extended with "Watchdog and disk-space guard" section (watchdog mechanics, force-clear procedure, disk-space precheck error contract, transactional eviction invariant, health probe usage + install). Production `ssc-companion` was **not restarted** — changes sit on disk for explicit rollout. **Not yet committed.**
- **2026-04-15** — WS05 (Cold-storage robustness) → `in_progress` (claude). No blocking deps (WS 06 is non-blocking). WS 04 has landed → adding `warming_started_at` column via new Alembic revision `0002_warming_started_at`.
- **2026-04-15** — WS04 (Schema migrations) → `done` (claude). Introduced Alembic for `stanford-stroke` DB at `companion/alembic/`. Pinned `alembic==1.18.4`, `SQLAlchemy==2.0.48`, `Mako==1.3.11`, `MarkupSafe==3.0.3`. Wrote `alembic.ini` (sqlalchemy.url blank — env.py builds it from `.env` to avoid leaking secrets), `alembic/env.py` (loads `.env` via `python-dotenv`, supports `DATABASE_URL`/`DB_NAME` overrides for scratch DBs, applies `include_object` filter that excludes upstream raw tables `image_series`/`image_study`/`lvo_clinical_data` and dynamic labelled/snapshot tables from `--autogenerate`), and `alembic/versions/0001_baseline.py`. Replaced `init_db()` in `companion/app.py` (deleted ~205 lines of `INIT_SQL`+`MIGRATE_SQL` blobs) with a call to `alembic.command.upgrade(cfg, "head")` followed by the existing `ensure_labelled_tables()`. Verification (T7 gate): scratch DB schema diff vs prod is **clean — only `alembic_version` table differs (expected)**; `alembic upgrade head` is idempotent; `init_db()` works and is idempotent. **T4 deviation:** rather than splitting `MIGRATE_SQL` into one revision per historical block, the baseline captures the cumulative terminal state as a single revision — the historical migrations were guarded by `IF NOT EXISTS`/`DO $$ ... $$` blocks, the dedup step is data-dependent and one-shot, and prod will be onboarded via `alembic stamp 0001_baseline` (no DDL re-run). T5 (dedup data migration) is folded into the baseline for the same reason. Documented in `documentation/operations/schema_migrations.md` with the Add-a-Revision workflow and the production stamp procedure; linked from `documentation/context.md` and `documentation/reference/data_stores.md`. **Production rollout (T9) NOT executed** — requires a one-time `alembic stamp 0001_baseline` against the live DB plus `systemctl restart ssc-companion`; held for explicit operator approval. **Not yet committed.**
- **2026-04-15** — WS04 (Schema migrations) → `in_progress` (claude). Introducing Alembic for `stanford-stroke` DB only.
- **2026-04-15** — WS01 (Backup and DR) → `done` (claude). Tier 1 scripts/units written & validated; dry-run restore (T9 acceptance gate) PASSED: row counts match production for `stanford-stroke` (`image_series`, `annotations`, `users`, `label_definitions`, `image_study`) and for `orthanc_db` top tables (`metadata`, `maindicomtags`, `changes`, `dicomidentifiers`, `resources`). Freshness monitor passes fresh/stale tests. Scope adjusted: SQL-only on dev (`/DATA2/pg_backups/`); Tier 2 implemented but dormant. **Not yet committed.** Timers exist in-repo but are not installed in `/etc/systemd/system/` — the user needs to run the install block in `backup_strategy.md` §3 to enable them.
- **2026-04-15** — WS01 (Backup and DR) → `in_progress` (claude). Scope adjusted: SQL-only backups under `/DATA2/pg_backups/`; cold-archive mirror implemented but dormant (no `/DATA3` on dev host).
- **2026-04-15** — WS02 (Dependency pinning) → `done` (claude). Pinned all Python deps in both `requirements.txt` files from the live `pacs` conda env (`pip freeze`). Committed `stanford-stroke-pacs/environment.yml` via `conda env export --from-history` (python + ipykernel — all runtime deps come from pip). Pinned upstream Orthanc image in `orthanc-indexer-patched/Dockerfile` by digest (`orthancteam/orthanc@sha256:a055ccd…`). Rebuilt `ssc-orthanc:patched-indexer` and clean-restarted Orthanc — `RemoveMissingFiles=false` banner present, version 1.12.10. Updated install docs to `npm ci`. Created `documentation/operations/upgrading_dependencies.md` and linked it from `context.md`. `docker-compose.yml` was not modified: it already references the locally-built `ssc-orthanc:patched-indexer` tag, so the upstream pin in the Dockerfile is the reproducibility anchor. **Not yet committed.**
- **2026-04-15** — WS02 (Dependency pinning) → `in_progress` (claude). Resolving versions from live `pacs` conda env; pinning both requirements.txt files, exporting `environment.yml`, pinning Orthanc image by digest.
- **2026-04-15** — WS03 (Security hardening) → `done` (claude). T1: rewrote `_rebuild_snapshots` with `psycopg2.sql.Composable` for all dynamic identifiers/literals; added `LABEL_NAME_RE=^[A-Za-z][A-Za-z0-9_]{0,62}$` allowlist enforced both in `create_label_definition` and as a pre-flight check in the snapshot rebuild path; all 12 existing label names already conform (no grandfathering needed); regression test at `scripts/test_snapshot_sql.py` proves hostile payloads are neutralised. T2: added `_require_env()` helper guarding `JWT_SECRET`, `ORTHANC_ADMIN_USER/PASSWORD`, `DB_USER`, `DB_PASSWORD` — empty value now raises `RuntimeError` at import time. T3: introduced `cookie_secure` in `config.toml` (default `true`) wired through `config.py` → `set_cookie(secure=...)` at login and at sliding-refresh; verified Set-Cookie toggles the `Secure` attribute. T4: added `session_absolute_timeout_hours` (default 24); JWT now carries an `iat` claim preserved across sliding refresh; `decode_jwt` rejects tokens older than the absolute TTL. T5: pinned `slowapi==0.1.9` in `companion/requirements.txt`, wired a per-IP limiter with `login_rate_limit_per_5min` (default 10), custom 429 handler emits `Retry-After`; 12 sequential logins from one IP produced 10×401 followed by 2×429. T6: wrote `documentation/operations/secret_rotation.md` (JWT, Orthanc admin, DB) and linked it from `context.md`. T7 (CI anti-pattern grep) handed off to WS 07. Production `ssc-companion` on :8043 was **not restarted** — changes sit on disk for explicit rollout. **Not yet committed.**
- **2026-04-15** — scaffold created, 12 workstreams initialized as `todo`.
